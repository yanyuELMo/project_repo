train:
  epochs: 5
  batch_size: 8
  lr: 1e-4
  wd: 1e-4
  k_frames: 8
  seed: 42
  num_workers: 2
  cpu: false
  log_level: INFO

eval:
  agg: max  # max|mean|ratio
  clip_threshold: 0.5
  video_threshold: 0.5
  ratio_threshold: 0.5

model:
  name: temporal_avg_resnet18
  pretrained: true
